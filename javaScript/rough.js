// ========= QUESTION NO 1 ==========
// var sum = new_array.reduce((accumulator, currentValue)=>{
//     return accumulator + currentValue;
// })
// if(sum === target){
//      new_array.forEach((item , index)=>{
//         return indexes.push(index)
// }
//      )}
//      console.log('Find the index of target element' , indexes)

// ========= QUESTION NO 2 ==========

// var addTwoNumbers = function(l1, l2) {
//     var result = [];
//     var carry = 0;

//     for (let i = 0; i < l1.length ; i++) {
//         var sum = l1[i] + l2[i] + carry;
//         carry = Math.floor(sum / 10);
//         result.push(sum % 10);
//     }

//     console.log(result.reverse());
// };

// addTwoNumbers([2, 4, 3], [5, 6, 4]);

// addTwoNumbers([2,4,3], [5,6,4] )
// addTwoNumbers([9,9,9,9,9,9,9], [9,9,9,9])
